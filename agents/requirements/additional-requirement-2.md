1. For currency number, make sure that it always display 2 decimal places. use comma as thousand separator. For example, 1000000.00 should be displayed as 1,000,000.00
2. For index page with group by category, make date format to be like '14 Mar 2026, Sun'
3. For add expense page (only add expense, not edit), if user has selected date, it should be default selected date. If not selected date, it should be today.
4. For add expense page (only add expense, not edit), if user fill amount, it will select category automatically based on the most frequent category for that amount from last 3 months. If there is no category for that amount, it will select the first category. It will not change the category if user has already selected a category.
